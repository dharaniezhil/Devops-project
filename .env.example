# MongoDB Atlas Configuration
# Replace with your actual MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/fixitfast?retryWrites=true&w=majority

# Database Configuration
DB_NAME=fixitfast
COLLECTION_NAME=profiles

# Server Configuration
PORT=3000

# Security Configuration (optional)
JWT_SECRET=your-super-secret-jwt-key-here
BCRYPT_SALT_ROUNDS=12

# Application Configuration
NODE_ENV=development

# Instructions:
# 1. Copy this file to .env
# 2. Replace <username> and <password> with your MongoDB Atlas credentials
# 3. Update the cluster URL if different
# 4. Adjust other settings as needed

# Example MongoDB URI format:
# mongodb+srv://myuser:mypassword@cluster0.abc123.mongodb.net/fixitfast?retryWrites=true&w=majority